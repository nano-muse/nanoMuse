# local configuration & secrets
config/config.toml
config/*.local.toml
.env
.env.*
!.env.example

# runtime data
workspace/
*.db
*.enc
*.key
audit.jsonl
sessions/
logs/

# python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# web app sources (the production build in nanomuse/server/static IS committed)
web/node_modules/
web/dist/
web/.vite/
npm-debug.log*

# android app (the signing key lives outside the repo; see docs/android.md)
android/.gradle/
android/build/
android/app/build/
android/local.properties
android/keystore.properties
android/.kotlin/
*.jks
*.keystore
*.apk
*.aab

# editors / OS
.vscode/
.idea/
.DS_Store
*.swp

# uv lockfile: the project installs with `uv pip install -e .`, not `uv sync`
uv.lock
