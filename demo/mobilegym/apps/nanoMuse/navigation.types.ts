// The OS contract wants a per-app navigation type module. The shape is shared across
// apps, so like the built-in Browser app we re-export the platform's copy.
export * from '../../system/Calendar/navigation.types';
