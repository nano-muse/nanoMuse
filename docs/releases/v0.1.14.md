# nanoMuse 0.1.14 · Home

A fix release. Coming back to the app — from a notification, from the tool capsule, from Hands finishing a run — lands in its own home again, not on the OpenMinis chat screen.

## Fixed

- **The old chat screen over the home.** On a phone, every "open this session" entry point went through the upstream deep-link path, which pushed the OpenMinis chat screen on top of the home shell: no Muse header, no tabs, the old look, until you pressed back. In the middle of a Hands task — an approval to answer, or the app coming back when the run was done — it read as the app falling back to an older version. Those requests now open the session inside the home shell; wide windows keep the upstream list/detail route.
- *Launch session → New chat* no longer pushes the upstream chat on a warm return either; the home opens on the main chat by itself, as it already did on a cold start.

## Changed

- versionCode 15; installs over 0.1.13 without losing data.

## Install

Download `nanoMuse-0.1.14-arm64.apk` (Android 8.0 or newer, arm64) and install it over the previous version — same signing key, your data is kept. Verify: `sha256sum -c nanoMuse-0.1.14-arm64.apk.sha256`.

## Source

Based on [OpenMinis](https://github.com/OpenMinis/OpenMinis) 1.13 (GPL-3.0), modified since 2026-09-24. The complete corresponding source of this build is tag `v0.1.14` plus the `android/deps/proot` submodule ([nano-muse/proot](https://github.com/nano-muse/proot)). The whole repository is GPL-3.0-or-later. nanoMuse is not affiliated with Meta; Muse is a trademark of Meta Platforms, Inc.

<details>
<summary>简体中文</summary>

修复版。从通知、悬浮胶囊、或者「动手」跑完之后回到 App，落回它自己的主页，而不是 OpenMinis 的聊天页。

### 修复

- **主页上盖了一层旧聊天页。** 在手机上，所有「打开这个会话」的入口都走的是上游的深链接路径，它会把 OpenMinis 的聊天页压到主页之上：没有 Muse 的头部和底栏，是旧的样子，按返回才能退出。在「动手」任务中途——要你审批、或者跑完回到 App 时——看起来就像软件退回了老版本。现在这些请求都在主页里直接切到那个会话；宽屏设备保留上游的列表 / 详情布局。
- 「启动时 → 新对话」这项偏好在热启动返回时也不再压一层上游聊天页；主页自己就会打开主对话，冷启动时本来如此。

### 变更

- versionCode 15；覆盖安装 0.1.13 不丢数据。

### 安装

下载 `nanoMuse-0.1.14-arm64.apk`（Android 8.0+，arm64），覆盖安装，同一把签名，数据保留；校验：`sha256sum -c nanoMuse-0.1.14-arm64.apk.sha256`。

### 来源

基于 [OpenMinis](https://github.com/OpenMinis/OpenMinis) 1.13（GPL-3.0），自 2026-09-24 起修改；本版本的完整对应源码是 tag `v0.1.14` 加子模块 `android/deps/proot`（[nano-muse/proot](https://github.com/nano-muse/proot)）。整个仓库以 GPL-3.0-or-later 发布。nanoMuse 与 Meta 无关，Muse 是 Meta Platforms, Inc. 的商标。

</details>
