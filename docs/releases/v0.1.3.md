# 0.1.3 · alpha · Muse 的形状

打开是一个聊天，不是会话列表。一个主聊天、脸在最上面，旁聊收进抽屉，底部四个页签：聊天、点子、目标、资源库——Muse 在手机上的形状。OpenMinis 原有的一切都还在，只是从这几页进去。

## 新增

- **首页。** 竖屏手机上启动路由直接渲染 `NanoMuseHome`：四页签的外壳，聊天页始终留在其他页签底下，切页签不丢滚动位置、不丢输入框草稿、正在输出的流也不断。平板和横屏保留 OpenMinis 的双栏布局。深链接和通知照旧在上面盖一个全屏聊天。
- **主聊天。** 第一个会话成为主聊天并被记住；App 打开就是它，点聊天页签也回到它。头部是 Muse 的：浅色圆盘上的脸、贴在下巴下面的名字胶囊、左边圆形菜单键、右边圆形 ⋯。旁聊用紧凑头部——标题、菜单、⋯——标题照旧由第一轮对话自动生成。
- **抽屉。** 名字、「主要聊天」、带搜索的旁聊列表、一个归档图标进完整的会话列表（OpenMinis 的 `SessionListScreen`，路由 `nanomuse/all_chats`，选中后交回首页）、设置、新建。长按旁聊可设为主要聊天。
- **目标。** 一页三段：「追踪」（勾选框、最近一条记录、节奏、下次检查、进度；⋯ 里有打开对话 / 立即检查 / 暂停 / 删除）、「例程」（就是 OpenMinis 的定时任务——开关、时间摘要、立即运行、运行记录、编辑器、「全部例程」）、「创建目标」七个分类。建目标是一段对话：底部面板的「开始吧」往主聊天发一句开场，附两轮系统提示附言；模型问完问题，末尾给一个 ` ```nanomuse-goal ` 代码块，App 把它变成目标卡片和 `goals.json` 里的一条。每个目标有自己的会话和一个隐藏的间隔定时任务（`ScheduledTask` 新增 `hidden` / `goalId` / `intervalMinutes`），到点往那个会话追加一行「看看这个目标」；目标的上下文和汇报格式放在该会话的系统提示里，模型回的 ` ```nanomuse-goal-update ` 变成进度卡片并更新那一行。
- **点子。** 六组二十四条，来自 `assets/nanomuse/ideas.{zh,en}.json`：表情、一句话、它会做什么，面板里只有一个主动作——发到聊天、创建例程（预填好的定时任务，直接进编辑器）、或在某个分类下开始目标。
- **资源库。** 「构件」「影音内容」两段，列出 agent 写在 `minis-sessions/*/workspace` 和 `minis-global/shared` 里的文件，新的在前，类型图标、图片缩略图、来自哪个对话；预览走 OpenMinis 的文件预览，分享走 file provider；⋯ 里有共享文件夹和主聊天的文件。
- `nanomuse-*` 围栏代码块在流式和静态两个 Markdown 渲染器里都渲染成卡片。

## 变更

- 第一次对话和目标的钩子现在在「重试」「继续」和排队消息之后也会跑，不只是普通发送之后。
- 定时任务列表隐藏目标检查；它们在目标页管理。
- nanoMuse 自己的页面用 Muse 的中性色（`MuseTones`）：白色表面、脸下面暖色圆盘、灰色填充；OpenMinis 自己的页面维持它的 iOS 分组配色。
- versionCode 4；覆盖安装 0.1.2 不丢数据，第一个已有会话自动成为主聊天。

## 有意保留

- 会话列表、定时任务和它的编辑器、共享文件夹、文件浏览和预览、终端、模型分组和所有设置页都没动，从新页面都能到。

## 已知问题

- 目标检查依赖模型按格式回一个 ` ```nanomuse-goal-update ` 块；模型不照做时对话照常进行，只是进度不更新。
- 点子里的例程用点子的标题做名字，可以在紧接着打开的编辑器里改。
- 资源库最多列 600 个文件、目录深度 4，不含以点开头的目录和 `node_modules` 之类。
- 只有 arm64 包；x86 模拟器请自行用 `-Pnm.abi=x86_64` 打测试包。

## 安装

下载 `nanoMuse-0.1.3-arm64.apk`（Android 8.0+，arm64），覆盖安装；校验：`sha256sum -c nanoMuse-0.1.3-arm64.apk.sha256`。

## 来源

基于 [OpenMinis](https://github.com/OpenMinis/OpenMinis) 1.13（GPL-3.0），自 2026-09-24 起修改；本版本的完整对应源码是 tag `v0.1.3` 加子模块 `android/deps/proot`（[nano-muse/proot](https://github.com/nano-muse/proot)）。整个仓库以 GPL-3.0-or-later 发布。nanoMuse 与 Meta 无关，Muse 是 Meta Platforms, Inc. 的商标。
